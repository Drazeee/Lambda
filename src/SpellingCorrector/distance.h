#ifndef DISTANCE_H
#define DISTANCE_H

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <math.h>

int distance (char* a,char* b);

#endif

#ifndef MAX_SIZE
#define MAX_SIZE 50
#endif